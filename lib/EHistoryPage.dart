import 'package:flutter/material.dart';

class EHistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ehistory Page'),
      ),
      body: Center(
        child: Text('Content of E-history page'),
      ),
    );
  }
}

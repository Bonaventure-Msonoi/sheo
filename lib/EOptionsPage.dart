import 'package:flutter/material.dart';

class EOptionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EOptions Page'),
      ),
      body: Center(
        child: Text('Content of E-Options page'),
      ),
    );
  }
}

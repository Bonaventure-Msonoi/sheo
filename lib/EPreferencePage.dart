import 'package:flutter/material.dart';

class EPreferencePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('E-preference Page'),
      ),
      body: Center(
        child: Text('Content of E-preference page'),
      ),
    );
  }
}
